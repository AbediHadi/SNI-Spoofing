اموزش وصل شدن با اندروید تو محیط ترموکس:

Warn: پیش نیاز اصلی این روش روت بودنه

ثبل زدن دستورات اگه ترموکس دسترسی روت نداره با زدن su دسترسی روت بدین بهش.
دستورات رو به ترتیب وارد کنید.

pkg update

pkg upgrade

pkg install python

pkg install git

termux-setup-storage

git clone https://github.com/Rainman69/SNISPF.git
cd SNISPF 
pip install .

وارد v2rayng شیذ و ادرس و پورت رو بزارید روی 127.0.0.1:40443 تو تنظیمات هم از حالت VPN تغییر بدید به PROXY 
وارد تلگرام یا هر اپی که میخواید شید و با پروکسی ساکس 127.0.0.1:1080 وصل شید و استفاده کنید.

